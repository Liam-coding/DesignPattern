package org.itstack.structureModel.adaptor.service;

public class POPOrderService {
    public boolean isFirstOrder(String uId) {
        System.out.println("POP merchant,query if the user order is the first one " + uId);
        return true;
    }
}
